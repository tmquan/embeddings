# Nemotron Embedding Extraction Pipeline
# Install: pip install -r requirements.txt
# Note: torch must be installed separately with CUDA support:
#   pip install torch==2.10.0+cu130 torchvision==0.25.0+cu130 --index-url https://download.pytorch.org/whl/cu130

# Core ML
transformers==4.45.2
accelerate==1.12.0
sentencepiece==0.2.1
einops==0.8.2
huggingface-hub==0.36.2

# Data processing
numpy==2.3.5
pandas==2.3.3
pyarrow==23.0.0

# Distributed / multi-GPU
ray==2.53.0

# NeMo Curator pipeline (used by 02_embedding_extraction_nemocurator.py)
nemo-curator==1.0.0
loguru==0.7.3